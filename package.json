{
  "name": "locking-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@openzeppelin/contracts": "^4.4.0",
    "ethers": "^5.5.2",
    "hardhat": "^2.7.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "local": "npx hardhat compile && npx hardhat run --network localhost ./scripts/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "lint-staged": {
    "*.{js,css,md,json,yml,yaml}": "prettier --write"
  }
}
